Building Instructions:
 * got to the directory build.<host>, where <host> is your working OS which
 * do a "make depend" to build depenencies (DO IT!)
 * do a "make nachos" to build NachOS

Usage:
do "./nachos -K" for run the simulation
